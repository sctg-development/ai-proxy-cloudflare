{
  "name": "ai-proxy-cloudflare",
  "version": "2.0.0",
  "description": "AI Proxy Cloudflare Worker — routes requests through Cloudflare AI Gateway with ai.json.enc decryption",
  "type": "module",
  "author": "Ronan LE MEILLAT",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest",
    "typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.0",
    "@cloudflare/workers-types": "^4.20260430.1",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "4.1.5",
    "wrangler": "^4.88.0"
  }
}
